enum HttpMethod { get, post, put, patch, delete }

enum SnackbarType { success, error, warning, info }

enum AuthEntryMode { phone, email }

enum RestaurantFilters { top_rated, open_now, certified, top_reviewed }