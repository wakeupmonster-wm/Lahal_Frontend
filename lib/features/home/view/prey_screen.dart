import 'package:flutter/material.dart';

class PreyScreen extends StatelessWidget {
  const PreyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("Prey Screen")));
  }
}
